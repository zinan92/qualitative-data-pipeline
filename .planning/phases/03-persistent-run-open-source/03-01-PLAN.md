---
phase: 03-persistent-run-open-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - scripts/park-intel-service.sh
  - scripts/install-service.sh
  - scripts/uninstall-service.sh
  - scripts/service-status.sh
  - com.park-intel.agent.plist
autonomous: true
requirements: [SHIP-01, SHIP-02, SHIP-03, SHIP-07, SHIP-08]

must_haves:
  truths:
    - "launchctl list shows com.park-intel.agent after install"
    - "Killing uvicorn process causes launchd to restart it automatically"
    - "CORS rejects requests from non-localhost origins by default"
    - "python main.py in dev still gets reload behavior"
    - "No /Users/ hardcoded paths in any tracked file"
  artifacts:
    - path: "com.park-intel.agent.plist"
      provides: "launchd LaunchAgent plist template"
      contains: "KeepAlive"
    - path: "scripts/park-intel-service.sh"
      provides: "Wrapper script activating venv and starting uvicorn"
      contains: "source .venv/bin/activate"
    - path: "scripts/install-service.sh"
      provides: "Copies plist with resolved paths to ~/Library/LaunchAgents"
      contains: "launchctl bootstrap"
    - path: "scripts/uninstall-service.sh"
      provides: "Stops and removes the LaunchAgent"
      contains: "launchctl bootout"
    - path: "scripts/service-status.sh"
      provides: "Shows service status via launchctl"
    - path: "main.py"
      provides: "Conditional reload + restricted CORS"
      contains: "PARK_INTEL_DEV"
  key_links:
    - from: "com.park-intel.agent.plist"
      to: "scripts/park-intel-service.sh"
      via: "ProgramArguments references wrapper script"
      pattern: "park-intel-service.sh"
    - from: "scripts/install-service.sh"
      to: "com.park-intel.agent.plist"
      via: "sed replaces __PROJECT_DIR__ with actual path"
      pattern: "__PROJECT_DIR__"
    - from: "main.py"
      to: "CORS_ORIGINS env var"
      via: "os.getenv for allow_origins"
      pattern: "CORS_ORIGINS"
---

<objective>
Make park-intel run as a persistent background service via macOS launchd, with production-safe defaults for CORS and reload behavior.

Purpose: The pipeline should survive reboots and crashes without manual intervention, and production config should not expose wildcard CORS or dev-mode reload.
Output: launchd plist, management scripts, hardened main.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistent-run-open-source/03-CONTEXT.md
@main.py
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production config in main.py + CORS hardening + path audit</name>
  <files>main.py</files>
  <read_first>main.py, config.py, .env</read_first>
  <action>
Modify main.py to address three concerns:

1. **CORS hardening (per D-08):** Replace the hardcoded `allow_origins=["*"]` (line 65) with environment-driven config. Read `CORS_ORIGINS` env var (comma-separated), defaulting to `["http://localhost:5174", "http://localhost:8001", "http://127.0.0.1:8001"]`. In config.py, add:
   ```python
   CORS_ORIGINS: list[str] = [
       o.strip()
       for o in os.getenv("CORS_ORIGINS", "http://localhost:5174,http://localhost:8001,http://127.0.0.1:8001").split(",")
       if o.strip()
   ]
   ```
   In main.py, import CORS_ORIGINS from config and use it in `allow_origins=CORS_ORIGINS`.

2. **Conditional reload (per D-07):** Change the `if __name__ == "__main__"` block at line 77-78. Replace `reload=True` with `reload=os.getenv("PARK_INTEL_DEV", "") == "1"`. Add `import os` at top of main.py.

3. **Hardcoded path audit (per D-12):** Run grep across all tracked files for `/Users/` patterns. The CONTEXT.md confirms none exist in Python or frontend. Verify no new ones were introduced. If any found, replace with relative paths or env vars.

Do NOT change any other behavior in main.py. Keep all router registrations, lifespan, and logging exactly as-is.
  </action>
  <verify>
    <automated>cd /Users/wendy/work/trading-co/park-intel && python -c "from config import CORS_ORIGINS; assert 'http://localhost:5174' in CORS_ORIGINS; print('CORS_ORIGINS ok:', CORS_ORIGINS)" && python -c "import main; mw = [m for m in main.app.user_middleware if 'CORSMiddleware' in str(m)]; print('CORS middleware registered')" && grep -rn '/Users/' --include='*.py' --include='*.ts' --include='*.tsx' --include='*.sh' . | grep -v node_modules | grep -v .venv | grep -v __pycache__ | grep -v .planning && echo "FAIL: hardcoded paths found" || echo "No hardcoded paths"</automated>
  </verify>
  <acceptance_criteria>
    - `from config import CORS_ORIGINS` works and returns list with localhost:5174
    - main.py CORSMiddleware uses CORS_ORIGINS variable, not `["*"]`
    - `reload=` in __main__ block reads PARK_INTEL_DEV env var
    - `grep -rn '/Users/' . --include='*.py'` returns zero matches (excluding .venv, __pycache__, .planning)
  </acceptance_criteria>
  <done>CORS restricted to localhost by default, reload gated behind PARK_INTEL_DEV=1, no hardcoded absolute paths in codebase</done>
</task>

<task type="auto">
  <name>Task 2: launchd plist + wrapper script + management scripts</name>
  <files>com.park-intel.agent.plist, scripts/park-intel-service.sh, scripts/install-service.sh, scripts/uninstall-service.sh, scripts/service-status.sh</files>
  <read_first>main.py, config.py, .planning/phases/03-persistent-run-open-source/03-CONTEXT.md</read_first>
  <action>
Create 5 files for persistent service management:

1. **com.park-intel.agent.plist** (per D-01, D-02, D-04, D-05) — LaunchAgent plist template stored in repo root. Use `__PROJECT_DIR__` as placeholder for absolute path (resolved at install time). Structure:
   - Label: `com.park-intel.agent`
   - ProgramArguments: `["__PROJECT_DIR__/scripts/park-intel-service.sh"]`
   - WorkingDirectory: `__PROJECT_DIR__`
   - KeepAlive: true (per D-02, satisfies SHIP-03)
   - StandardOutPath: `__PROJECT_DIR__/logs/park-intel-stdout.log`
   - StandardErrorPath: `__PROJECT_DIR__/logs/park-intel-stderr.log`
   - RunAtLoad: true
   - Do NOT include hardcoded /Users/ paths — only __PROJECT_DIR__ placeholder

2. **scripts/park-intel-service.sh** (per D-03) — Wrapper script:
   - `#!/bin/bash`
   - `set -euo pipefail`
   - `cd "$(dirname "$0")/.."`  (navigate to project root)
   - `source .venv/bin/activate`
   - `exec python -m uvicorn main:app --host 127.0.0.1 --port 8001`
   - No `--reload` flag (per D-03)
   - Make executable: chmod +x

3. **scripts/install-service.sh** (per D-06):
   - Resolve PROJECT_DIR to `$(cd "$(dirname "$0")/.." && pwd)`
   - Create logs/ directory
   - Copy plist to ~/Library/LaunchAgents/com.park-intel.agent.plist
   - Use `sed` to replace `__PROJECT_DIR__` with actual resolved path
   - Run `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.park-intel.agent.plist`
   - Print success message with `scripts/service-status.sh` hint
   - Make executable

4. **scripts/uninstall-service.sh** (per D-06):
   - `launchctl bootout gui/$(id -u)/com.park-intel.agent` (ignore error if not loaded)
   - Remove ~/Library/LaunchAgents/com.park-intel.agent.plist
   - Print confirmation
   - Make executable

5. **scripts/service-status.sh** (per D-06):
   - Run `launchctl print gui/$(id -u)/com.park-intel.agent 2>&1` and parse output
   - Show PID, state, last exit status
   - If not found, print "Service not installed"
   - Make executable

All scripts must use `#!/bin/bash` and `set -euo pipefail`. No hardcoded /Users/ paths anywhere.
  </action>
  <verify>
    <automated>cd /Users/wendy/work/trading-co/park-intel && test -f com.park-intel.agent.plist && echo "plist exists" && grep -q "KeepAlive" com.park-intel.agent.plist && echo "KeepAlive present" && grep -q "__PROJECT_DIR__" com.park-intel.agent.plist && echo "placeholder present" && ! grep -q "/Users/" com.park-intel.agent.plist && echo "no hardcoded paths in plist" && test -x scripts/park-intel-service.sh && echo "wrapper executable" && grep -q "uvicorn" scripts/park-intel-service.sh && echo "uvicorn in wrapper" && ! grep -q "\-\-reload" scripts/park-intel-service.sh && echo "no reload flag" && test -x scripts/install-service.sh && test -x scripts/uninstall-service.sh && test -x scripts/service-status.sh && echo "all management scripts executable" && bash -n scripts/park-intel-service.sh && bash -n scripts/install-service.sh && bash -n scripts/uninstall-service.sh && bash -n scripts/service-status.sh && echo "all scripts pass syntax check"</automated>
  </verify>
  <acceptance_criteria>
    - com.park-intel.agent.plist exists with KeepAlive=true, RunAtLoad=true, __PROJECT_DIR__ placeholders
    - scripts/park-intel-service.sh is executable, activates .venv, runs uvicorn without --reload
    - scripts/install-service.sh resolves paths, copies plist, runs launchctl bootstrap
    - scripts/uninstall-service.sh runs launchctl bootout and removes plist
    - scripts/service-status.sh shows service state
    - All scripts pass `bash -n` syntax check
    - No /Users/ hardcoded in any of the created files
  </acceptance_criteria>
  <done>launchd service infrastructure complete: plist template, wrapper, install/uninstall/status scripts all created and syntax-verified</done>
</task>

</tasks>

<verification>
1. `python -c "from config import CORS_ORIGINS; print(CORS_ORIGINS)"` returns localhost origins
2. `grep -c 'allow_origins=\["\\*"\]' main.py` returns 0 (wildcard CORS removed)
3. All 5 new files exist and pass bash syntax check
4. `grep -rn '/Users/' . --include='*.py' --include='*.sh' --include='*.plist' | grep -v .venv | grep -v __pycache__ | grep -v .planning` returns empty
5. `pytest tests/ -x -q` still passes (no regressions)
</verification>

<success_criteria>
- CORS defaults to localhost origins, configurable via CORS_ORIGINS env var
- reload=True only activates when PARK_INTEL_DEV=1
- launchd plist with KeepAlive=true ensures auto-restart on crash
- Management scripts (install/uninstall/status) work with resolved paths
- Zero hardcoded /Users/ paths in tracked files
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistent-run-open-source/03-01-SUMMARY.md`
</output>
