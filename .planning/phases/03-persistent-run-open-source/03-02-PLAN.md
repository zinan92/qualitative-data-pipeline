---
phase: 03-persistent-run-open-source
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - README.md
autonomous: false
requirements: [SHIP-04, SHIP-05, SHIP-06, SHIP-09]

must_haves:
  truths:
    - ".env.example lists every environment variable with documentation"
    - "Core sources (RSS, HackerNews, Reddit) collect articles without any API keys"
    - "Optional sources skip gracefully with a clear log message when tokens are missing"
    - "README quick-start gets a fresh clone to a running server in 3 commands"
  artifacts:
    - path: ".env.example"
      provides: "Documented template of all environment variables"
      contains: "ANTHROPIC_API_KEY"
    - path: "README.md"
      provides: "Open-source README with quick start, architecture, source config"
      contains: "Quick Start"
  key_links:
    - from: ".env.example"
      to: "config.py"
      via: "Every os.getenv() call in config.py has a matching line in .env.example"
      pattern: "ANTHROPIC_API_KEY|XUEQIU_COOKIE|GITHUB_TOKEN|CORS_ORIGINS|PARK_INTEL_DEV"
    - from: "README.md"
      to: "scripts/install-service.sh"
      via: "README documents launchd setup as optional section"
      pattern: "install-service"
---

<objective>
Package park-intel for open-source release: documented env template, verified zero-config core sources, graceful degradation for optional sources, and a rewritten README for an unfamiliar developer.

Purpose: Clone-to-run experience is the #1 open-source adoption factor. A developer should go from `git clone` to seeing data in the health dashboard without configuring any API keys.
Output: .env.example, updated README.md, verified source behavior
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistent-run-open-source/03-CONTEXT.md
@config.py
@README.md
@.env
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: .env.example + verify core sources + graceful skip for optional sources</name>
  <files>.env.example</files>
  <read_first>config.py, .env, .gitignore, collectors/rss.py, collectors/hackernews.py, collectors/reddit.py, collectors/xueqiu.py, collectors/github_release.py, sources/adapters.py</read_first>
  <action>
1. **Create .env.example (per D-09, SHIP-04):** Scan config.py for every `os.getenv()` call. Create .env.example with all variables documented. Group by category:

```
# park-intel environment variables
# Copy to .env and fill in values for optional sources

# === Core (no keys needed) ===
# RSS, HackerNews, and Reddit work without any API keys.

# === Optional: LLM Tagging ===
# Powers AI relevance scoring. Without it, articles still collect but won't have relevance_score.
# ANTHROPIC_API_KEY=sk-ant-...

# === Optional: Xueqiu (Chinese market KOL posts) ===
# Get cookie from browser dev tools after logging into xueqiu.com
# XUEQIU_COOKIE=

# === Optional: GitHub ===
# Increases rate limit for GitHub release/trending collectors
# GITHUB_TOKEN=ghp_...

# === Server Configuration ===
# CORS_ORIGINS=http://localhost:5174,http://localhost:8001,http://127.0.0.1:8001
# PARK_INTEL_DEV=0
```

Include CORS_ORIGINS and PARK_INTEL_DEV (from Plan 01 changes). Every variable is commented out by default so a fresh `.env` copy works immediately.

2. **Verify .env.example is in .gitignore allowlist:** Confirm .gitignore excludes `.env` but does NOT exclude `.env.example`. Current .gitignore has `.env` and `.env.local` -- .env.example is fine (not matched).

3. **Verify core sources work without keys (per D-10, SHIP-05):** Read collectors/rss.py, collectors/hackernews.py, collectors/reddit.py. Confirm they use no env vars / API keys. The CONTEXT.md says "already functional" -- verify by reading the collector code. If any use env vars, document the finding.

4. **Verify optional source graceful skip (per D-11, SHIP-06):** Read collectors for xueqiu, github_release, and social_kol. Check that when their required env vars are empty/missing, they:
   - Do NOT crash
   - Log a clear message explaining why they're skipping
   - Return an empty list

   If any collector is missing a graceful skip message, add a clear `logger.warning("[%s] Skipping collection: XUEQIU_COOKIE not configured", self.source)` (or equivalent) at the top of the collect() method before returning []. Do NOT change the collector's logic otherwise.
  </action>
  <verify>
    <automated>cd /Users/wendy/work/trading-co/park-intel && test -f .env.example && echo ".env.example exists" && grep -q "ANTHROPIC_API_KEY" .env.example && echo "has ANTHROPIC_API_KEY" && grep -q "XUEQIU_COOKIE" .env.example && echo "has XUEQIU_COOKIE" && grep -q "GITHUB_TOKEN" .env.example && echo "has GITHUB_TOKEN" && grep -q "CORS_ORIGINS" .env.example && echo "has CORS_ORIGINS" && grep -q "PARK_INTEL_DEV" .env.example && echo "has PARK_INTEL_DEV" && python -c "from collectors.hackernews import HackerNewsCollector; print('HN collector imports without env vars')" && python -c "from collectors.rss import RSSCollector; print('RSS collector imports without env vars')" && pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .env.example exists and contains documented entries for: ANTHROPIC_API_KEY, XUEQIU_COOKIE, GITHUB_TOKEN, CORS_ORIGINS, PARK_INTEL_DEV
    - All entries are commented out (lines start with #) so fresh copy works
    - Core source collectors (rss, hackernews, reddit) have zero env var dependencies confirmed
    - Optional source collectors log a warning and return [] when tokens missing
    - pytest passes with no regressions
  </acceptance_criteria>
  <done>.env.example documents all variables, core sources verified keyless, optional sources skip gracefully with log messages</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README for open-source audience</name>
  <files>README.md</files>
  <read_first>README.md, .env.example, CLAUDE.md, .planning/phases/03-persistent-run-open-source/03-CONTEXT.md</read_first>
  <action>
Rewrite README.md for an open-source audience (per D-14, D-15, D-16, SHIP-09). The current README is in Chinese and oriented toward the developer. The new README must work for an unfamiliar English-speaking developer.

Structure (per D-14):

1. **Header:** Project name "park-intel", one-line description, badges (Python, FastAPI, React, tests)

2. **What it does:** 2-3 sentences. Self-hosted market intelligence pipeline. Collects from 10+ sources, enriches with keyword/LLM tagging, clusters into narrative events, serves via REST API + feed UI.

3. **Quick Start** (per D-14 -- 3 commands):
```bash
git clone https://github.com/zinan92/intel.git park-intel
cd park-intel

# Backend
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # edit .env to add optional API keys
python main.py                # starts on http://localhost:8001

# Frontend
cd frontend && npm install && npm run dev
# open http://localhost:5174
```

4. **Core vs Optional Sources:** Table showing which sources need API keys and which don't.
| Source | Key Required | Env Var |
|--------|-------------|---------|
| RSS Feeds | No | - |
| Hacker News | No | - |
| Reddit | No | - |
| GitHub Releases | Optional | GITHUB_TOKEN |
| Xueqiu (Chinese market) | Yes | XUEQIU_COOKIE |
| LLM Tagging | Optional | ANTHROPIC_API_KEY |

5. **Architecture Overview:** The pipeline diagram from CLAUDE.md (Source Registry -> Adapters -> Collectors -> SQLite -> LLM Tagger -> API -> Frontend). Keep it as ASCII art.

6. **Health Dashboard:** Mention /health page shows source status, freshness, volume anomalies. Reference screenshot per D-15: `![Health Dashboard](docs/health-dashboard.png)` (note: screenshot may not exist yet, use the reference anyway).

7. **Run as Background Service (macOS)** (per D-16): Optional section documenting launchd setup:
```bash
./scripts/install-service.sh    # installs and starts
./scripts/service-status.sh     # check status
./scripts/uninstall-service.sh  # stop and remove
```

8. **API Endpoints:** Brief list of key endpoints (health, feed, events, articles).

9. **Development:** How to run tests (`pytest tests/`), dev mode (`PARK_INTEL_DEV=1 python main.py`).

10. **Contributing:** Standard open-source section (fork, branch, PR).

11. **License:** MIT (or note to add one).

Keep the README under 300 lines. Write in English. Do not include Chinese text. Do not include the Xueqiu KOL list or internal implementation details.
  </action>
  <verify>
    <automated>cd /Users/wendy/work/trading-co/park-intel && test -f README.md && echo "README exists" && grep -q "Quick Start" README.md && echo "has Quick Start" && grep -q "pip install" README.md && echo "has pip install" && grep -q "install-service" README.md && echo "has launchd section" && grep -q "ANTHROPIC_API_KEY" README.md && echo "mentions env vars" && wc -l README.md | awk '{if ($1 < 350) print "line count ok:", $1; else print "WARN: too long:", $1}'</automated>
  </verify>
  <acceptance_criteria>
    - README.md is in English, written for an unfamiliar developer
    - Contains "Quick Start" section with git clone + pip install + python main.py
    - Contains source configuration table (core vs optional)
    - Contains architecture overview
    - Contains "Run as Background Service" section referencing install-service.sh
    - Contains API endpoints section
    - Under 350 lines total
    - No Chinese text (except source names like "Xueqiu" which are proper nouns)
  </acceptance_criteria>
  <done>README rewritten for open-source audience with quick start, architecture, source config, launchd docs, and API reference</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify quick-start flow and README quality</name>
  <files>README.md, .env.example</files>
  <action>Present the completed .env.example and README.md to the user for review. No code changes -- this is a human verification gate.</action>
  <what-built>
    - .env.example with all documented environment variables
    - README.md rewritten for open-source audience
    - Core sources verified to work without API keys
    - Optional sources verified to skip gracefully
  </what-built>
  <how-to-verify>
    1. Open README.md and read the Quick Start section -- does it make sense for someone who has never seen this project?
    2. Check .env.example -- are all variables documented clearly?
    3. Verify the health dashboard: open http://localhost:8001/health (if server running) and confirm core sources show data
    4. Check that the architecture section accurately describes the pipeline
  </how-to-verify>
  <verify>User approves the README and .env.example content</verify>
  <done>User confirms README is clear for an unfamiliar developer and .env.example is complete</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `.env.example` exists with all env vars from config.py documented
2. Core collectors (RSS, HN, Reddit) confirmed to require zero API keys
3. Optional collectors log warning and return empty when tokens missing
4. README.md under 350 lines, in English, has Quick Start + Architecture + Source Config + launchd docs
5. `pytest tests/ -x -q` passes
</verification>

<success_criteria>
- A developer reading only the README can go from git clone to running server
- .env.example serves as complete documentation of all configuration options
- Core sources collect data on a fresh clone with empty .env
- Optional sources degrade gracefully without crashing
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistent-run-open-source/03-02-SUMMARY.md`
</output>
